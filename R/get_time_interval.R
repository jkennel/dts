# 
# meta <- dts$trace_time
# 
# get_time_interval <- function(x) {
# 
#   list(start = sort(unique(diff(sort(meta$start)))),
#        end   = sort(unique(diff(sort(meta$end)))))
# 
# }
# 
# get_time_interval(meta)


